spring:
  datasource:
    username: root
    password: yxx
    url: jdbc:mysql://localhost:3306/competitive?useUnicode=true&characterEncoding=utf8&useAffectedRows=true
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  configuration:
    map-underscore-to-camel-case: true
shiro:
  loginUrl: /login.html
  sessionManager:
  # disable URL session rewriting
    sessionIdUrlRewritingEnabled: false
logging:
  level:
    com:
      competitive:
        dao=debug: